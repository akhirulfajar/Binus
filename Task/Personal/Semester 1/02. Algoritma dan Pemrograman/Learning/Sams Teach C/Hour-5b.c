#include <stdio.h>

int main() 
{

    /* putchar is the same as putc, but it only need one argument, (without stating stdout) */
    putchar(65);
        putchar(10);
            putchar(66);
                putchar(10);
            putchar(67);
        putchar(10);
    return 0;
}